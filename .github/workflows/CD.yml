name: Continuous Deployment
on:
  push:
    branches:
      - master
jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: Run slow tests
        run: mvn test -DskipTests=false -Dgroups=slow

